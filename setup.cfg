# add the files in examples/ to those that pytest should collect
[tool:pytest]
python_files = test_*.py *_test.py examples/*.py example_*.py
python_functions = test_* example_*
filterwarnings =
    ignore::pandas.errors.PerformanceWarning
    ignore::PendingDeprecationWarning
